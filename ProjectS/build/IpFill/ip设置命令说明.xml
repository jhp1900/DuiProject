<!-- 设置 ip 地址自动获取 -->
netsh interface ip set address "以太网" dhcp

<!-- 设置 DNS 地址自动获取 -->
netsh interface ip set dnsservers "以太网" dhcp

<!-- 设置固定的 IP地址 -->
netsh interface ip set address "以太网" static 10.10.10.110 255.255.255.0 10.10.10.1

<!-- 设置固定的 DNS地址 -->
netsh interface ip set dnsservers "以太网" static 10.10.10.1

<!-- 添加多个 IP地址 -->
netsh interface ip add address "以太网" 172.20.134.122 255.255.255.0 172.20.134.20

<!-- 添加多个 DNS地址 -->
netsh interface ip add dnsservers "以太网" 8.8.8.8



  <!-- 
    说明：
      netsh：即 Network Shell ，网络配置必不可少的命令。
      interface: 命令解释，进入接口配置模式，可对 ip 进行增删改查等操作
      set address: 设置 ip
      set dnsservers: 设置 DNS
      "以太网": 这里的双引号可以不要；这里的名字可能不同，可能是 "本地连接"
      dhcp: 这里填写 dhcp 表示是自动连接
      
    注意：
      添加多个 ip 或者 dns 的前提是已设置了 ip 或者 dns ；
      在添加多个 ip 或者 dns 的时候，不能使用 static 参数.
  -->
  
